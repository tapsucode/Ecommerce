server:
  port: 5454
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/ecommerce
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

